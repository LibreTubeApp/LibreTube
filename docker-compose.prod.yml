version: '3'

services:
  db:
    restart: always
    environment:
      POSTGRES_PASSWORD: hunter12
      POSTGRES_USER: nextube
      POSTGRES_DB: nextube

  web:
    restart: always
    ports:
      - "80:3000"
    environment:
      #EXTERNAL_URL: http://your-url.com:1234
      NODE_ENV: production
      DBHOST: db
      DBUSER: nextube
      DBDATABASE: nextube
      DBPASSWORD: hunter12
